version: "3"

services:
  api:
    container_name: api
    restart: unless-stopped
    build:
      context: ./
    logging:
      driver: "json-file"
      options:
        max-size: 10m
        max-file: "2"
    command: "python start.py"
